---
tags: [hexlet, java, испытание]
source: "Java Корпоративные приложения на Spring Boot"
---

# Конечный автомат

## Задание

Все испытания

#  Java: Конечный автомат 

Java: Корпоративные приложения на Spring Boot 1 сообщение

Начать испытание 

Конечный автомат – это абстрактная машина, которая в один момент времени может находиться только в одном из конечного числа состояний. Конечный автомат может переходить из одного состояния в другое, этот процесс называется переход.

В этом задании вам нужно будет самим создать конечный автомат для поста в блоге. Перед началом работы рекомендуем изучить следующие материалы:

  * Урок по конечным автоматам из курса "Автоматное программирование"
  * Статья "Как спроектировать правильный конечный автомат"



## src/main/java/exercise/model/PostState.java

Изучите код в файле _PostState.java_. В перечислении `PostState` содержатся состояния, в котором может находиться пост

## src/main/java/exercise/model/Post.java

Создайте конечный автомат для поста, опираясь на описание его работы. У поста может быть три состояния - создан (CREATED), опубликован (PUBLISHED) и заархивирован (ARCHIVED):

  * CREATED - начальное состояние, в котором создаётся новый пост. Пост находится на модерации и после проверки может быть опубликован или отправлен в архив.
  * PUBLISHED - пост опубликован. Из этого состояния пост может быть только отправлен в архив
  * ARCHIVED - пост находится в архиве



Есть два события:

  * publish – публикация поста. Выполняется при помощи PATCH запроса на адрес _/posts/1/publish_
  * archive – архивация поста. Выполняется при помощи PATCH запроса на адрес _/posts/1/archive_


  1. Создайте метод `publish()`, который публикует статью. Метод переводит пост в состояние `PUBLISHED`. Переход в состояние `PUBLISHED` возможен только из состояния `CREATED`. Если переход из текущего состояния в целевое возможен, метод устанавливает новое состояние и возвращает `true`, как успешность выполнения операции. Если переход не возможен, метод возвращает `false`.

  2. Создайте метод `archive()`, который отправляет статью в архив. Метод переводит пост в состояние `ARCHIVED`. Переход в состояние `ARCHIVED` возможен из состояний `CREATED` и `PUBLISHED`. Если переход из текущего состояния в целевое возможен, метод устанавливает новое состояние и возвращает `true`, как успешность выполнения операции. Если переход не возможен, метод возвращает `false`.




## src/main/java/exercise/controller/PostController.java

  1. Изучите метод контроллера `publish()`, который выполняет публикацию поста.

  2. Допишите метод `archive()`, который переводит статью в архив

  3. Изучите метод `getPosts()`, который возвращает список всех постов. Обратите внимание, что он возвращает только опубликованные посты.




Запустите приложение командой `make start` и откройте Web-доступ в упражнении, чтобы увидеть результат работы приложения

## Подсказки

  * Для наглядности нарисуйте на бумаге диаграмму состояний поста
  * Изменения, которые вы сделаете в исходном коде после старта приложения, не будут подтягиваться автоматически. Чтобы учесть сделанные изменения, остановите приложение и вновь запустите его командой `make start`
  * Чтобы отправлять запросы приложению, можете использовать любую утилиту для работы с HTTP, например Postman. Url приложения скопируйте из адресной строки браузера на странице Web-доступ в упражнении

## Код

**AppApplication.java**
```java
package io.hexlet.app;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class AppApplication {

    public static void main(String[] args) {
        SpringApplication.run(AppApplication.class, args);
    }
}
```

**Initializer.java**
```java
package io.hexlet.app.component;

import java.util.List;

import io.hexlet.app.model.PostState;
import io.hexlet.app.repository.PostRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.ApplicationArguments;
import org.springframework.boot.ApplicationRunner;
import org.springframework.stereotype.Component;


import lombok.AllArgsConstructor;

import io.hexlet.app.model.Post;

@Component
@AllArgsConstructor
public class Initializer implements ApplicationRunner {

    @Autowired
    private PostRepository postRepository;

    @Override
    public void run(ApplicationArguments args) throws Exception {
        addModels();
    }

    private void addModels() {
        var posts = List.of(
                new Post("The Moving Toyshop", "Why is it that when one man builds a wall", PostState.CREATED),
                new Post("Death Be Not Proud", "Laughter is poison to fear", PostState.PUBLISHED),
                new Post("Dying of the Light", "Give me honorable enemies", PostState.ARCHIVED),
                new Post("The Monkey`s Raincoat", "Do the dead frighten you?", PostState.PUBLISHED),
                new Post("The World", "When the snows fall, the lone wolf dies", PostState.ARCHIVED)
        );

        postRepository.saveAll(posts);
    }
}
```

**Post.java**
```java
package io.hexlet.app.model;

import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

import jakarta.persistence.Column;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.Entity;
import jakarta.persistence.Table;
import jakarta.persistence.Id;
import jakarta.persistence.Lob;
import jakarta.persistence.GenerationType;

@Getter
@Setter
@Entity
@Table(name = "posts")
@NoArgsConstructor
public class Post {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private long id;

    @Column(unique = true)
    private String title;

    @Lob
    private String body;

    public Post(String title, String body, PostState state) {
        this.title = title;
        this.body = body;
        this.state = state;
    }

    private PostState state = PostState.CREATED;



    // BEGIN (write your solution here)
    
    // END
}
```

**PostController.java**
```java
package io.hexlet.app.controller;

import io.hexlet.app.dto.PostCreateDto;
import io.hexlet.app.exception.PostNotFoundException;
import io.hexlet.app.exception.UnprocessableEntityException;
import io.hexlet.app.model.Post;
import io.hexlet.app.model.PostState;
import io.hexlet.app.repository.PostRepository;
import lombok.RequiredArgsConstructor;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.PatchMapping;

@RestController
@RequestMapping("/posts")
@RequiredArgsConstructor
public class PostController {

    @Autowired
    private final PostRepository postRepository;

    @GetMapping(path = "")
    public Iterable<Post> getPosts() {
        return postRepository.findAllByState(PostState.PUBLISHED);
    }

    @PostMapping(path = "")
    public Post createPost(@RequestBody PostCreateDto dto) {
        Post post = new Post();
        post.setBody(dto.getBody());
        post.setTitle(dto.getTitle());

        return postRepository.save(post);
    }

    @GetMapping(path = "/{id}")
    public Post getPost(@PathVariable long id) {
        return postRepository.findById(id)
            .orElseThrow(() -> new PostNotFoundException(id));
    }

    @PatchMapping(path = "/{id}/publish")
    public Post publish(@PathVariable long id) {
        Post post = postRepository.findById(id)
            .orElseThrow(() -> new PostNotFoundException(id));


        if (post.publish()) {
            return postRepository.save(post);
        }

        throw new UnprocessableEntityException("Publishing is not possible");
    }

    // BEGIN (write your solution here)
    
    // END
}
```

**PostCreateDto.java**
```java
package io.hexlet.app.dto;

import lombok.Getter;
import lombok.Setter;

@Getter
@Setter
public class PostCreateDto {
    private String title;
    private String body;
}
```

**PostNotFoundException.java**
```java
package io.hexlet.app.exception;

import org.springframework.web.bind.annotation.ResponseStatus;
import org.springframework.http.HttpStatus;

@ResponseStatus(HttpStatus.NOT_FOUND)
public class PostNotFoundException extends RuntimeException {
    public PostNotFoundException(long id) {
        super("Post " + id + " not found");
    }
}
```

**PostState.java**
```java
package io.hexlet.app.model;

public enum PostState {
    CREATED,
    PUBLISHED,
    ARCHIVED
}
```

**UnprocessableEntityException.java**
```java
package io.hexlet.app.exception;

import org.springframework.web.bind.annotation.ResponseStatus;
import org.springframework.http.HttpStatus;

@ResponseStatus(HttpStatus.UNPROCESSABLE_ENTITY)
public class UnprocessableEntityException extends RuntimeException {
    public UnprocessableEntityException(String message) {
        super(message);
    }
}
```

**WelcomeController.java**
```java
package io.hexlet.app.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class WelcomeController {

    @GetMapping("/")
    public String root() {
        return "Welcome to Spring";
    }
}
```

## Моё решение

```java
// напиши решение здесь
```
