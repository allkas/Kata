---
tags: [hexlet, java, испытание]
source: "Java Стримы"
---

# Одинаковая четность

## Задание

Все испытания

#  Java: Одинаковая четность 

Java: Стримы

Начать испытание 

## scr/main/java/io/hexlet/App.java

В классе `App` реализуйте публичный статический метод `sameParityFilter`, который принимает в качестве параметра список `List` целых чисел. Метод должен вернуть новый список, состоящий из элементов, у которых такая же четность, как и у первого элемента входного списка
    
    
    List<Integer> coll1 = List.of(-1, 0, 1, -3, 10, -2);
    List<Integer> result1 = App.sameParityFilter(coll1);
    System.out.println(result1); // [-1, 1, -3]
    
    List<Integer> coll2 = List.of(2, 0, 1, -3, 10, -2);
    List<Integer> result2 = App.sameParityFilter(coll2);
    System.out.println(result2); // [2, 0, 10, -2]
    
    List<Integer> emptyColl = new ArrayList<>();
    List<Integer> result3 = App.sameParityFilter(emptyColl);
    System.out.println(result3); // []

## Код

**App.java**
```java
package io.hexlet;

import java.util.List;
import java.util.ArrayList;

class App {
    // BEGIN (write your solution here)
    
    // END
}
```

## Моё решение

```java
// напиши решение здесь
```
