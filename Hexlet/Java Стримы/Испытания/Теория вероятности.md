---
tags: [hexlet, java, испытание]
source: "Java Стримы"
---

# Теория вероятности

## Задание

Все испытания

#  Java: Теория вероятности 

Java: Стримы 1 сообщение

Начать испытание 

Игральная кость – шестигранный кубик, который бросается несколько раз

## scr/main/java/io/hexlet/App.java

В классе `App` реализуйте публичный статический метод `calculateProbabilities()`, который принимает на вход историю подбрасывания кубика в виде списка `List` целых чисел и возвращает словарь `Map`. Ключом этого объекта служит число из списка, а значением – ещё один словарь, в котором ключи – это числа, выпавшие сразу после первоначального числа, а значения – вероятность их выпадения.

Например, если передать на вход список из чисел `1, 3, 1, 5, 1`, итоговый объект будет выглядеть так:
    
    
    // Вывод Map отформатирован для наглядности
    {
      1={3=0.5,5=0.5},
      3={1=1.0},
      5={1=1.0}
    }
    

После числа 1 выпадали числа 3 и 5 с равной долей вероятности 0.5. А после чисел 3 и 5 всегда выпадала единица, что даёт нам вероятность в 1.
    
    
    // Пустой список на входе
    Map<Integer, Map<Integer, Double>> result1 = App.calculateProbabilities(
        new ArrayList<Integer>()
    );
    System.out.println(result1); // => {}
    
    Map<Integer, Map<Integer, Double>> result2 = App.calculateProbabilities(
        List.of(1, 3, 1, 5, 1, 2, 1, 6)
    );
    System.out.println(result2);
    // => {1={2=0.25, 3=0.25, 5=0.25, 6=0.25}, 2={1=1.0}, 3={1=1.0}, 5={1=1.0}, 6={}}

## Код

**App.java**
```java
package io.hexlet;

import java.util.stream.Collectors;
import java.util.stream.IntStream;
import java.util.Map;
import java.util.List;
import java.util.function.Function;

class App {
    // BEGIN (write your solution here)
    
    // END
}
```

## Моё решение

```java
// напиши решение здесь
```
